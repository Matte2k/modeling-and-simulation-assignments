function [abOptions] = abSettings(order,method,alpha,beta)
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

abOptions = struct;
    abOptions.order = order;
    abOptions.method = method;
    abOptions.alpha = alpha;
    abOptions.beta = beta;
end

